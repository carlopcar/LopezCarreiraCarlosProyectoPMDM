<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lopezcarreiracarlosproyectopmdm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_app"
        android:roundIcon="@drawable/ic_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.LopezCarreiraCarlosProyectoPMDM">
        <activity
            android:name=".activities.EditarActivity"
            android:exported="true"
            android:label="Fábrica de peliculas"/>
        <activity
            android:name=".activities.DetallePeliculaActivity"
            android:exported="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.PeliculasActivity" />
        </activity>
        <activity
            android:name=".activities.PeliculasActivity"
            android:exported="true"
            android:label="Listado de Peliculas"/>
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>